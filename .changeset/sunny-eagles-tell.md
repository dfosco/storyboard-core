---
'storyboard': minor
---

Bundle optimizations: pre-parse data at build time, split vendor chunks (react, primer, octicons, reshaped), remove unused Primer theme CSS variants (714KB to 244KB), add changesets for versioning
